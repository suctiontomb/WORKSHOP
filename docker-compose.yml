version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: workshop
      POSTGRES_USER: workshop_user
      POSTGRES_PASSWORD: fleet123
    ports:
      - "4532:5432"
    volumes:
      - postgres_data:var/lib/postgresql/data
    networks:
      - workshop_network
  web:
    build: .
    command: "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      - DATABSE_URL=postgresql://workshop_user:workshop123@db:5432/workshop
    networks:
      - workshop_network
networks:
  workshop_network:

volumes:
  postgres_data:










